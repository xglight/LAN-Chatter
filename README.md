# 局域网聊天器

## 前言

v1.0 为命令行版本，其实并没有什么用。
也许可以给入门级的小白一个参考。

## 介绍

全部使用 C++ 编写，基于 TCP 协议，采用 winsock2 库实现。

其中 `server.cpp` 为服务器端，`client.cpp` 为客户端。

## 使用

可以在 release 页面下载编译好的可执行文件，也可以自己编译。

先在一台电脑上开启 `server.exe`，再在其他电脑上开启 `client.exe`，输入服务端 IP 地址即可开始聊天。